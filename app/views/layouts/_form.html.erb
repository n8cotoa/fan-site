<%= form_for @post do |f| %>
  <%= f.label :username %>
  <%= f.collection_select :user_id, User.order(:username),:id, :username %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>
<% end %>
